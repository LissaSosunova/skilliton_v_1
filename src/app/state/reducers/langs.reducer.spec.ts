import { initialState } from './langs.reducer';

describe('Langs Reducer', () => {
  describe('an unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;
    });
  });
});
