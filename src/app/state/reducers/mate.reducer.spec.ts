import { mateReducer, initialState } from './mate.reducer';

describe('Mate Reducer', () => {
  describe('an unknown action', () => {
    it('should return the previous state', () => {
      const action = {} as any;
    });
  });
});
