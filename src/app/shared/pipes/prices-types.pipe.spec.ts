import { PricesTypesPipe } from './prices-types.pipe';

describe('PricesTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new PricesTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
