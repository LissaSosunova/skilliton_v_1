import { EducationLevelsPipe } from './education-levels.pipe';

describe('EducationLevelsPipe', () => {
  it('create an instance', () => {
    const pipe = new EducationLevelsPipe();
    expect(pipe).toBeTruthy();
  });
});
