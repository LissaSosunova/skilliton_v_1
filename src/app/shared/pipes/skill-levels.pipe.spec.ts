import { SkillLevelsPipe } from './skill-levels.pipe';

describe('SkillLevelsPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillLevelsPipe();
    expect(pipe).toBeTruthy();
  });
});
